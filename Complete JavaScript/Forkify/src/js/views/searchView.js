import { elements } from './base';

export const getInput = () => elements.searchInput.value;
renderRecipes = recipe => {

}
export const renderResults = recipes => {
    recepies.forEach(renderRecipes);
}